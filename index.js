'use strict';

var machines = require('./machines'),
    viewers = require('./viewers'),
    repos = require('./repos');

module.exports = {
    machines: machines,
    viewers: viewers,
    repos: repos
};
